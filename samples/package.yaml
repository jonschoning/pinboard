name: sample
version: '0.1.0.0'
author: Jon Schoning
maintainer: jonschoning@gmail.com
license: MIT
other-extensions:
- TemplateHaskell
dependencies:
- base >=4.6 && <5.0
- lens >=4.13
- transformers >=0.4.0.0
- pinboard
- base >=4.6 && <5.0
- transformers >=0.4.0.0
- aeson
- text
- bytestring
- http-types >=0.8 && <0.13
- http-client >=0.5 && <0.6
- http-client-tls >=0.3.0 && <0.4
executables:
  sampleLens:
    main: sampleLens.hs
    source-dirs: ./.
    ghc-options: -Wall
  sample:
    main: sample.hs
    source-dirs: ./.
    ghc-options: -Wall
